# Dataset

The dataset used in this project is `Dataset-SA.csv`, containing customer reviews and their corresponding sentiment labels.

The original dataset is approximately 205,052 records in size and was used for sentiment analysis in this project.

The complete CSV file is not included in this GitHub repository because it exceeds GitHub's file upload size limit.

The dataset was used locally in Google Colab during the implementation of the project.
